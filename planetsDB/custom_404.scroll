import header.scroll
title Oops! - Page not found

* Sorry but we cannot find the requested page.

css
 .scrollFilePageComponent { text-align: center;}

import searchForm.scroll

html <script>document.getElementById("searchFormInput").value = (window.location.pathname + "").split("/").pop().replace(/\.[^\.]+$/, "").replace(/</g, "&lt;") </script>

* You can report a broken link on the GitHub.

import footer.scroll